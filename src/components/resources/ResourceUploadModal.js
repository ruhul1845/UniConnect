import Modal from '../ui/Modal';
import { Button } from '../ui/Button';
import { Field, inputClass, selectClass } from '../ui/Form';
import { resourceTypes } from './resourceUtils';
export default function ResourceUploadModal({ form, setForm, file, setFile, uploading, onClose, onSubmit }) {
  const update = (key, value) => setForm({ ...form, [key]: value });
  return <Modal title="Upload Academic Resource" eyebrow="CR Upload" onClose={onClose}><form onSubmit={onSubmit} className="space-y-5"><div className="grid gap-4 md:grid-cols-2"><Field label="Title"><input className={inputClass} required value={form.title} onChange={(e) => update('title', e.target.value)} /></Field><Field label="Type"><select className={selectClass} value={form.resource_type} onChange={(e) => update('resource_type', e.target.value)}>{resourceTypes.map(([v, l]) => <option key={v} value={v}>{l}</option>)}</select></Field><Field label="Course Code"><input className={inputClass} required value={form.course_code} onChange={(e) => update('course_code', e.target.value)} /></Field><Field label="Batch"><input className={inputClass} required value={form.batch} onChange={(e) => update('batch', e.target.value)} /></Field><Field label="Semester"><input className={inputClass} required value={form.semester} onChange={(e) => update('semester', e.target.value)} /></Field><Field label="Year"><input className={inputClass} value={form.year} onChange={(e) => update('year', e.target.value)} /></Field><Field label="Lecture No / Author"><input className={inputClass} value={form.lecture_no || form.author} onChange={(e) => setForm({ ...form, lecture_no: e.target.value, author: e.target.value })} /></Field><Field label="PDF File"><input className={inputClass} type="file" accept="application/pdf" onChange={(e) => setFile(e.target.files?.[0] || null)} /></Field></div><Field label="Description"><textarea className={inputClass} rows="3" value={form.description} onChange={(e) => update('description', e.target.value)} /></Field><div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={onClose}>Cancel</Button><Button disabled={uploading}>{uploading ? 'Uploading...' : 'Upload & Publish'}</Button></div></form></Modal>;
}
